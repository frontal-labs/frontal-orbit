# Development Dockerfile with hot reload
FROM rust:1.75-bookworm@sha256:87f3b2f93b82995443a1a558c234212dafe79cfdc3af956539610560369ddcd0

# Install development dependencies
RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        ca-certificates \
        git \
        libssl-dev \
        pkg-config \
        sqlite3 \
        libsqlite3-dev \
        libpq-dev \
        curl \
        build-essential \
        postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# Install cargo-watch for hot reload
RUN cargo install cargo-watch --locked

# Set working directory
WORKDIR /workspace

# Create non-root user
RUN useradd --create-home --shell /bin/bash orbit

# Switch to non-root user
USER orbit

# Environment variables
ENV CARGO_TERM_COLOR=always
ENV ORBIT_HOME=/workspace/.orbit
ENV SANDBOX_HOME=/workspace/.sandbox-home

# Default command with hot reload
CMD ["cargo", "watch", "-x", "run"]
