//! {{name}} crate — generated by `orbit generators crate`.

pub fn hello() -> &'static str {
    "{{name}}"
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        assert_eq!(hello(), "{{name}}");
    }
}
