//! {{name}} binary — generated by `orbit generators crate`.

fn main() {
    println!("{}", hello());
}

pub fn hello() -> &'static str {
    "{{name}}"
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        assert_eq!(hello(), "{{name}}");
    }
}
