apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: orbit

resources:
  - namespace.yaml
  - configmap.yaml
  - persistentvolumeclaims.yaml
  - orbit-server.yaml
  - orbit-slack.yaml
  - ingress.yaml

generatorOptions:
  disableNameSuffixHash: true
